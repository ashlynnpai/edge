.navigation
  %ul.nav.navbar-nav
    - if current_user
      %h2= "Hello #{current_user.name}" 

    %li= link_to 'See All Courses', courses_path
    %li= link_to 'New Course', new_course_path
    %li= link_to 'Profile', user_path('happy-hippo')

    %li= link_to 'Register', register_path
    %li= link_to 'Login', login_path
    - if logged_in?
      %li= link_to 'Logout', logout_path

    - if current_user
      %li= link_to 'Courselist', courseitems_path(current_user)
    
